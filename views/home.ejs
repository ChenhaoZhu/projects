<%- include("partials/header"); -%>

  <h1>Knorth's Home</h1>
  <p> <%= startingContent %></p>
  <% console.log(posts); %>


  <% posts.forEach(function(post){ %>
      <h1><%= post.title %></h1>
      <p><%= post.content.substring(0, 100) + "......" %></p>
      <a href="/posts/<%= post.title%>">Details Here</a>
    <% }) %>


<%- include("partials/footer"); -%>
